---
description: Open the sidewrite dashboard in your browser (starts the viewer daemon first if needed).
allowed-tools: Bash(node:*)
---

!`node ${CLAUDE_PLUGIN_ROOT}/scripts/cli.cjs open`

Reply in ONE short line: confirm the dashboard is open at the printed `http://127.0.0.1:<port>` URL (or relay the error). Do NOT add extra explanation — keep the response minimal so it returns instantly.
