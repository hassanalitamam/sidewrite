---
description: Alias of /sidewrite-delegate — delegate the current task to a headless sidewrite provider run.
argument-hint: [task note]
allowed-tools: Bash, Write
---

Invoke the `sidewrite-delegate` skill now and follow its steps exactly. Keep all the run logic in the skill — do not duplicate it here.

Extra context: $ARGUMENTS
